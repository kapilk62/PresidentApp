<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.presidentapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Profile" android:label="Profile"></activity>
        <activity android:name=".Rules" android:label="Rules" />
        <activity android:name=".Gate_Keeper" android:label="Gate_Keeper"/>
        <activity android:name=".Vehicles" android:label="Vehicles"/>
        <activity android:name=".Complaints" android:label="Complaints"/>
        <activity android:name=".My_Building" android:label="My_Building"/>
        <activity android:name=".Emergency_Number" android:label="Emergency_Number"/>
        <activity android:name=".Society_Fund" android:label="Society_Fund"/>
        <activity android:name=".Members" android:label="Members"/>
        <activity android:name=".Bills" android:label="Bills"/>
        <activity android:name=".MainActivity" android:launchMode="standard" android:label="Dashboard" />
        <activity android:name=".LoginActivity" android:theme="@style/Theme.AppCompat.NoActionBar" android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" android:theme="@style/Theme.AppCompat.NoActionBar" android:launchMode="singleTask"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>